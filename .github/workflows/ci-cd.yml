name: 🚀 TechFood API - Deploy Only

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

env:
  NAMESPACE: techfood
  AWS_REGION: us-east-1

jobs:
  deploy-to-eks:
    name: 🚀 Deploy API to EKS
    runs-on: ubuntu-latest
    if: github.event_name == 'push'

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v3

    - name: 🛠️ Setup kubectl
      uses: azure/setup-kubectl@v3
      with:
        version: 'latest'

    - name: 🛠️ Setup Kustomize
      run: |
        curl -s "https://raw.githubusercontent.com/kubernetes-sigs/kustomize/master/hack/install_kustomize.sh" | bash
        sudo mv kustomize /usr/local/bin/

    - name: ⚙️ Configure AWS credentials (AWS Academy)
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
        aws-region: ${{ env.AWS_REGION }}
        mask-aws-account-id: false

    - name: 🔍 Verify AWS credentials
      run: |
        echo "🔍 Verifying AWS credentials..."
        aws sts get-caller-identity
        echo "📋 Checking EKS cluster access..."
        aws eks describe-cluster --region ${{ env.AWS_REGION }} --name eks-techfood-terraform --query 'cluster.{Name:name,Status:status,Endpoint:endpoint}' --output table

    - name: 📋 Update kubeconfig for EKS
      run: |
        echo "📋 Updating kubeconfig for EKS cluster..."
        aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name eks-techfood-terraform
        echo "✅ Kubeconfig updated successfully"
        kubectl config current-context
        kubectl cluster-info --request-timeout=30s

    - name: 🔄 Update image references
      run: |
        cd src/base

        # Update image reference to use Docker Hub
        DOCKERHUB_USER="${{ secrets.DOCKERHUB_USERNAME }}"

        # Use kustomize to set the correct image
        kustomize edit set image techfood-api=${DOCKERHUB_USER}/techfood-api:latest

        echo "📋 Updated image reference:"
        cat kustomization.yaml

    - name: 🚀 Deploy using base/kustomization.yaml
      run: |
        echo "🚀 Deploying techfood-api using base/kustomization.yaml..."
        echo "📦 Using image: ${{ secrets.DOCKERHUB_USERNAME }}/techfood-api:latest"

        # Apply resources using kustomize from base directory
        echo "📋 Deploying resources from src/base/kustomization.yaml..."
        kustomize build src/base | kubectl apply -f -

        echo "⏳ Waiting for deployment rollout..."
        kubectl rollout status deployment/techfood-api -n ${{ env.NAMESPACE }} --timeout=300s

    - name: ✅ Deployment Summary
      run: |
        echo "🎉 TechFood API deployed successfully!"
        echo "🏷️ Image: ${{ secrets.DOCKERHUB_USERNAME }}/techfood-api:latest"
        echo "📅 Deployed at: $(date)"
        kubectl get pods -l app.kubernetes.io/name=techfood-api -n ${{ env.NAMESPACE }}
